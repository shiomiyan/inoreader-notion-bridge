{
	"name": "inoreader-notion-bridge",
	"version": "0.0.0",
	"private": true,
	"packageManager": "pnpm@11.0.8",
	"engines": {
		"node": "24.x"
	},
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest run",
		"cf-typegen": "wrangler types",
		"check": "wrangler types --check && tsc --noEmit && biome check ./src ./test",
		"format": "biome check --write ./src ./test",
		"lint": "biome lint ./src ./test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.14",
		"@cloudflare/vitest-pool-workers": "^0.16.3",
		"typescript": "^6.0.3",
		"vitest": "~4.1.5",
		"wrangler": "^4.78.0"
	},
	"dependencies": {
		"hono": "^4.12.18"
	}
}
